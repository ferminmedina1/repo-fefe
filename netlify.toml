[build]
  command = "npm ci --include=optional --no-audit --no-fund && npm install --no-save --include=optional @rollup/rollup-linux-x64-gnu@4.59.0 --no-audit --no-fund && npm run build"
  publish = "dist"

[build.environment]
  NPM_FLAGS = "--include=optional"
